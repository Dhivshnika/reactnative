import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    heading: {
        fontSize: 30,
        textAlign: 'center',
        marginTop: 150,
    },
    output: {
        fontSize: 25,
        textAlign: 'center',
        marginTop: 30,
    },
    buttonWrapperView: {
        flexDirection: 'row',
        marginTop: 30
    },
    buttonInc: {
        fontSize: 20,
        marginLeft: 60,
    },
    buttonDec: {
        fontSize: 20,
        marginLeft: 40,
    }
})

export default styles;